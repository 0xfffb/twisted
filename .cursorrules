# Twisted Project Rules

反爬虫实验项目：**ES5 源码 → Hyperion IR → 字节码 → TypeScript 同步 VM**。

## 项目结构（当前）

```text
src/
  compiler/       HyperionCompiler、白名单、IR（value/）
  assembler/      HyperionAssembler → bytecode + meta
  obfuscator/     assemble 后加固（Opcode 置换、meta 加密）
  builder/        HyperionBuildBundle、runtime、bench
  vm/             同步解释器
  cli.ts          check / bench / build / dump / runtime / all
example/          示例脚本（fingerprint.js）
tests/            单测
docs/             语法白名单、IR、Worker 约定、todos
```

## 代码规范

### TypeScript

- ES 模块（`import`/`export`）
- 类名 PascalCase，方法 camelCase，常量 UPPER_SNAKE_CASE
- 业务逻辑用中文注释；避免无关改动

### 编译器

- 输入须 **ES5**；`@babel/parser` 只解析，**不**降级
- 白名单：`src/compiler/whitelist.ts`；拒绝时 `CompileError`
- 生产路径仅 **HyperionCompiler → HyperionAssembler**

### 虚拟机

- 同步执行；`Opcode` 定义在 `src/constant.ts`
- 宿主回调：`MakeClosure` 为真 JS 函数，保留 `this`
- 加固 bundle：`protection.seed` + `RuntimeProtection` 派生 map/key

### 混淆

- **字节码**：`src/obfuscator` Pass 管线（assemble 之后）
- **外层 JS**：`javascript-obfuscator`（`runtime` / `all` 的 `--obfuscate`）
- 勿在 compiler 内做 meta 加密

## 测试与构建

- `npm test`：compiler / vm / this-binding / obfuscator / check
- `npm run cli -- check <file>`：语法门禁
- `npm run bench`：体积与 VM 执行采样
- `npm run build:pages`：CI 生成 `public/runtime.js`（默认**未** hardened）

## 添加新 Opcode

1. `src/constant.ts`（`Opcode` + `OPCODE_NAMES`）
2. `HyperionCompiler` / IR 指令
3. `HyperionAssembler` 降低与编码
4. `src/obfuscator/layout.ts`  arity 表
5. `src/vm/vm.ts` handler

## 提交

- 中文提交信息：`<类型>: <描述>`
